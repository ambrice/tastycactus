#!/usr/bin/env ruby

compile '/stylesheets/*' do
  filter :sass
end

route '/stylesheets/*' do
  item.identifier.chop + '.css'
end

compile '*' do
  filter :haml
  layout 'default'
end

route '*' do
  item.identifier + 'index.html'
end

layout '*', :haml
